//#FILENAME:pkg/services/pubsub/subscriber/{{snakeCase data.metadata.name}}-consumer.go:create-only
package subscriber

import (
    "errors"
{{#isEntityType data.spec.payloadType.type}}
    "{{@root.options.basePackage}}/generated/entities"
{{/isEntityType}}
    "github.com/kapetacom/sdk-go-config/providers"
)

type {{pascalCase data.metadata.name}}Consumer struct{}

func New{{pascalCase data.metadata.name}}Consumer(cfg providers.ConfigProvider) (*{{pascalCase data.metadata.name}}Consumer, error) {
    return &{{pascalCase data.metadata.name}}Consumer{}, nil
}

func (c *{{pascalCase data.metadata.name}}Consumer) OnMessage(message {{variableType data.spec.payloadType.type prefix=""}}, attributes map[string]string) error {
    if true {
        return errors.New("not implemented yet")
    }
    return nil
}
