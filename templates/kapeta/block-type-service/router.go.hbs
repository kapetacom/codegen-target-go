//#FILENAME:generated/router.go:write-always
package generated
import (
    "github.com/labstack/echo/v4"
    rest "{{@root.options.basePackage}}/generated/rest_api"
    kapeta "github.com/kapetacom/sdk-go-config/providers"
)

func RegisterRouters(e *echo.Echo, cfg kapeta.ConfigProvider) error {
    var err error
    {{#providers-of-type 'kapeta/resource-type-rest-api'}}
        {{#kaplang-rest-methods spec.source namespace=metadata.name}}
            err = rest.Create{{controller-name this}}Router(e, cfg);
            if err != nil {
                return err
            }
        {{/kaplang-rest-methods}}
    {{/providers-of-type}}

    err = rest.CreateHealth(e);
    if err != nil {
        return err
    }

    return nil
}
